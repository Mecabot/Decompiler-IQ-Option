package com.neovisionaries.ws.client;

/* compiled from: PerMessageCompressionExtension */
abstract class s extends ah {
    protected abstract byte[] t(byte[] bArr);

    protected abstract byte[] x(byte[] bArr);

    public s(String str) {
        super(str);
    }
}
